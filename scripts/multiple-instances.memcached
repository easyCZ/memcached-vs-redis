#!/bin/bash

DIR=threads

for i in {1..30}
do
    python main.py \
        -type=memcached \
        -server-conf=../configs/$DIR/memcached.$i.conf \
        -memtier-conf=../configs/$DIR/memtier.memcached.conf \
        -output=../results/$DIR/memcached/$i/
done
