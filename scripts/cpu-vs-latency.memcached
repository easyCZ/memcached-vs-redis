#!/bin/bash

for i in {1..25}
do
    for j in {1..3}
    do
        python main.py \
            -type=memcached \
            -server-conf=../configs/cpu-vs-latency2/memcached.conf \
            -memtier-conf=../configs/cpu-vs-latency2/memtier.memcached.$i.conf \
            -output=../results/cpu-vs-latency2/memcached/$i-$j/
    done
done
