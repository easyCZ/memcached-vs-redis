#!/bin/bash

for i in {1..25}
do
    for j in {1..3}
    do
        python main.py \
            -type=memcached \
            -server-conf=../configs/cpu-vs-latency3/memcached.conf \
            -memtier-conf=../configs/cpu-vs-latency3/memtier.memcached.$i.conf \
            -output=../results/cpu-vs-latency3/memcached/$i-$j/
    done
done
