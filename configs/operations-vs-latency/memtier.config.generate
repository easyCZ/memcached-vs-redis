#!/bin/bash

LABEL=memcached

mkdir $LABEL

for i in {80..560..80}
do
    echo "-s nsl200
-p 11120
--requests=$((i * 75))
-c 1
-t 1
-P memcache_binary
--random-data
--key-minimum=200
--key-maximum=400
" > memcached/memtier.memcached.$i.conf
done
