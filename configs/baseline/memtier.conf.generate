#!/bin/bash

LABEL=multiple-instances

mkdir $LABEL

for i in {1..12}
do
    echo "-s nsl200
-p 11120
--test-time=30
-c $(( (16 / $i) + 1))
-t 4
-P memcache_binary
--random-data
--key-minimum=100
--key-maximum=10000" > $LABEL/memtier.memcached.$i.conf
done
