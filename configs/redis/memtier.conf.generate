#!/bin/bash

LABEL=instances-higher
mkdir $LABEL
KEY_MAX=10000

for i in {1..10}
do
    echo "-s nsl200
-p 11120
--test-time=30
-c $((6 / $i))
-t 2
-P redis
--random-data
--key-minimum=100
--key-maximum=$(($KEY_MAX / $i))" > $LABEL/memtier.redis.$i.conf

done
