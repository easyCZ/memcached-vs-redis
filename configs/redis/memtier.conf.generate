#!/bin/bash

LABEL=instances-36
mkdir $LABEL
KEY_MAX=10000

for i in {1..8}
do
    echo "-s nsl200
-p 11120
--test-time=30
-c $((6 / $i))
-t 2
-P redis
--random-data
--key-minimum=100
--key-maximum=$(($KEY_MAX / $i))" > $LABEL/memtier.redis.$i.conf

done
